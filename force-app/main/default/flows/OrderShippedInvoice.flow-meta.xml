<?xml version="1.0" encoding="UTF-8" ?>
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>65.0</apiVersion>
    <description
  >When an Order status changes to Shipped, Delivered, or Completed, generates and emails a PDF invoice.</description>
    <label>Order Status - Generate Invoice</label>
    <processType>AutoLaunchedFlow</processType>
    <status>Active</status>

    <!--
        FIX: <triggerType> belongs INSIDE <start>, not at root <Flow> level.
        <doesRequireRecordChangedToMeetCriteria> ensures this only fires
        when Status CHANGES to Shipped, not on every update.
    -->
    <start>
        <locationX>176</locationX>
        <locationY>0</locationY>
        <connector>
            <targetReference>Generate_Invoice</targetReference>
        </connector>
        <doesRequireRecordChangedToMeetCriteria
    >true</doesRequireRecordChangedToMeetCriteria>
        <filterLogic>1 OR 2 OR 3</filterLogic>
        <filters>
            <field>Status</field>
            <operator>EqualTo</operator>
            <value>
                <stringValue>Shipped</stringValue>
            </value>
        </filters>
        <filters>
            <field>Status</field>
            <operator>EqualTo</operator>
            <value>
                <stringValue>Delivered</stringValue>
            </value>
        </filters>
        <filters>
            <field>Status</field>
            <operator>EqualTo</operator>
            <value>
                <stringValue>Completed</stringValue>
            </value>
        </filters>
        <object>Order</object>
        <recordTriggerType>Update</recordTriggerType>
        <triggerType>RecordAfterSave</triggerType>
    </start>

    <actionCalls>
        <name>Generate_Invoice</name>
        <label>Generate Invoice</label>
        <locationX>176</locationX>
        <locationY>120</locationY>
        <actionName>InvoiceService</actionName>
        <actionType>apex</actionType>
        <inputParameters>
            <name>orderIds</name>
            <value>
                <elementReference>$Record.Id</elementReference>
            </value>
        </inputParameters>
    </actionCalls>

</Flow>
