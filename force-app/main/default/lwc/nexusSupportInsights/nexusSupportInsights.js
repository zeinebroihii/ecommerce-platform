import { LightningElement } from 'lwc';

/**
 * nexusSupportInsights — right-panel widget for the Support & Cases board.
 * Shows AI Support Summary (dark card) + Communication Trends (light card).
 * Self-contained with static AI-generated content.
 */
export default class NexusSupportInsights extends LightningElement {}
